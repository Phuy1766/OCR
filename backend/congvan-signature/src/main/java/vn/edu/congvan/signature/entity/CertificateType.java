package vn.edu.congvan.signature.entity;

/** Loại certificate. */
public enum CertificateType {
    /** Cert cá nhân — gắn với 1 user (lãnh đạo, cấp phó). */
    PERSONAL,
    /** Cert cơ quan — dùng cho VAN_THU_CQ ký con dấu điện tử. */
    ORGANIZATION
}
